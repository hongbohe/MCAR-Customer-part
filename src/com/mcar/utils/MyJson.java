package com.mcar.utils;

import org.json.JSONArray;
import org.json.JSONException;
import org.json.JSONObject;

import com.mcar.info.DriverLoc;

public class MyJson {
/*
	public DriverLoc getDriverLoc(String result) {
		DriverLoc driverLoc = null;
		try {
			JSONArray jay = new JSONArray(result);
			driverLoc = new DriverLoc();
				JSONObject job = jay.getJSONObject(0);
				DriverLoc info = new DriverLoc();
				info.setDriverLat(job.getDouble("LAT"));
				info.setDriverLng(job.getDouble("LNG"));
		} catch (JSONException e) {
		}
		return driverLoc;
	}*/
	
	
}
